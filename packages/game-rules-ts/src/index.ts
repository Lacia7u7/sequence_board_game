/**
 * Placeholder TypeScript implementation of Sequence game rules.
 *
 * Functions will be fleshed out in future commits.  The goal of this
 * package is to provide a pure, deterministic rule engine that can be
 * shared between the frontend and backend.
 */

export interface GameState {
  board: string[][];
}

export function validMoves(_state: GameState): string[] {
  return [];
}
